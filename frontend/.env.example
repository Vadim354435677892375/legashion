# URL бэкенда legashion (см. backend/README.md).
# Локально по умолчанию используется http://localhost:4000, эта переменная
# нужна в первую очередь на проде (Vercel) — там должен указывать на домен
# развёрнутого backend-проекта.
VITE_API_URL=http://localhost:4000
